<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.mapper.ModuleMapper">

    <resultMap type="com.ruoyi.web.domain.Module" id="ModuleResult">
        <result property="id" column="id"/>
        <result property="templateId" column="template_id"/>
        <result property="pageIndex" column="page_index"/>
        <result property="name" column="name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectModule">
        select id, template_id, page_index, name, create_time, update_time
        from module
    </sql>

    <select id="selectModuleByTemplateId" resultType="com.ruoyi.web.domain.Module">
        <include refid="selectModule"/>
        where template_id = #{templateId} and page_index = #{pageIndex}
    </select>


</mapper>